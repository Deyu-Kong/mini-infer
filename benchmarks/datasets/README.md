{
  "comment": "Synthetic ShareGPT-style sample, 1000 prompts. Used by benchmarks/bench_*.cpp via load_prompts_from_json. Real ShareGPT can be downloaded with `python scripts/download_sharegpt.py` (TODO).",
  "prompts": "see entries below; this is a one-object-per-line JSONL file (not a strict JSON array) so reviewers can `head -n 5 datasets/sharegpt_sample.jsonl` quickly"
}